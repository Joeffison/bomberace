[project]
name = "bomberace"
version = "0.1.0"
description = ""
authors = [
    {name = "Joeffison Silvério de Andrade",email = "joeffison@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame (>=2.6.1,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
black = "^25.1.0"
isort = "^6.0.1"


[tool.poetry.group.test.dependencies]
pytest = "^8.4.2"

